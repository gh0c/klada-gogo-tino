<% provide(:title, @team.short_name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= team_logo @team, size:80 %>
        <%= @team.name %>
      </h1>
    </section>
    <div>
      <span>Playoff: </span> 
      <span ><%= @team.playoff? ? "yes" : "no"  %></span>
    </div>
  </aside>

</div>