<%= render 'shared/error_messages', object: f.object %>

<%= f.label :name %>
<%= f.text_field :name, class: 'form-control' %>

<% if logged_in? && current_user.admin? && !current_user?(@user) %>
<%= f.label :admin %>
<%= f.check_box :admin, class: 'form-control-rb' %>
<% end %>
